# Optional override for the "Get in touch" form's submit endpoint.
# Defaults to FormSubmit relaying to kandenexus@gmail.com — see src/components/sections/Contact.tsx.
# Set to an empty value in .env.local to fall back to opening a mailto: draft instead (useful for local testing).
VITE_CONTACT_ENDPOINT=
